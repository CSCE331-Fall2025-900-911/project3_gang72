# Frontend (Login landing)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Set your Google Client ID in `frontend/.env` (do not commit):

```
VITE_GOOGLE_CLIENT_ID=YOUR_CLIENT_ID.apps.googleusercontent.com
```

The dev server proxies `/api` to `http://localhost:3000`.
